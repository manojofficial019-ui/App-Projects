⚠️ IMPORTANT: KEEP THIS FILE SECURE AND DELETE AFTER READING ⚠️

Your Android keystore has been created with the following details:

Keystore File: upload-keystore.jks
Key Alias: pomodoro-timer-key
Password: pomodoro2024

⚠️ SECURITY NOTES:
1. The password "pomodoro2024" is a TEMPORARY password
2. For production use, consider creating a new keystore with a stronger password
3. NEVER commit key.properties or *.jks files to version control
4. Keep backups of your keystore in a secure location
5. You'll need this keystore for ALL future app updates on Google Play
6. If you lose this keystore, you CANNOT update your app on Google Play Store

To change the password later, you can:
1. Create a new keystore with a stronger password
2. Update key.properties with the new password
3. Keep the old keystore as backup (you'll need it for existing app updates)

The keystore is valid for 10,000 days (approximately 27 years).




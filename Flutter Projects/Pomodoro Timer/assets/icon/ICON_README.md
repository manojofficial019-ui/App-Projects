# App Icon Setup

✅ App icon is configured! The icon file `pomodoro_app_icon.png` is already in place.

To generate the app icons for all platforms, run:
```
flutter pub get
flutter pub run flutter_launcher_icons
```

This will automatically generate all required icon sizes for:
- Android (multiple densities)
- iOS (all required sizes)
- Other platforms if configured

